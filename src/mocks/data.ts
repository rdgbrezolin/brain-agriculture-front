import initialData from "./initialData.json";

export const mockProducers = initialData.producers;
export const states = initialData.states;
export const crops = initialData.crops;
export const harvests = initialData.harvests;
